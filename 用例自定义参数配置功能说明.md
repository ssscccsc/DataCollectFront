# 用例自定义参数配置功能说明

## 功能概述

在采集策略管理页面的新增/编辑策略流程中，增加了**用例级别的自定义参数配置**功能。用户在选择用例集后，可以为每个测试用例单独配置自定义参数，实现更精细化的测试参数控制。

## 背景说明

### 原有功能

在此功能之前，采集策略管理页面已经支持**策略级别的自定义参数**：
- 在策略表单中可以配置全局自定义参数
- 这些参数会应用到该策略下的所有测试用例
- 适用于需要统一参数配置的场景

### 新增功能

现在新增了**用例级别的自定义参数配置**：
- 可以为每个测试用例单独配置自定义参数
- 用例级别参数与策略级别参数独立管理
- 提供更灵活的参数配置方式

## 功能特性

### 1. 用例参数状态显示

在用例列表表格中增加了"用例参数"列：

- ✅ **已配置状态**：显示绿色设置图标和参数数量徽章
- ✅ **未配置状态**：显示"未配置"文本
- ✅ **悬停提示**：鼠标悬停显示"已配置 N 个参数"

### 2. 参数配置对话框

点击"配置参数"按钮后，弹出专门的配置对话框：

#### 对话框结构

**用例信息区域**
- 用例名称（加粗显示）
- 用例编号（标签形式）
- 业务大类
- APP

**参数配置区域**
- 参数列表（支持添加/删除）
- 每个参数包含：参数键（key）和参数值（value）
- 序号标识（圆形蓝色编号）
- 删除按钮

#### 操作功能

- ✅ **添加参数**：点击"添加参数"按钮增加新参数行
- ✅ **删除参数**：点击删除按钮移除参数
- ✅ **保存参数**：保存按钮将参数保存到表单
- ✅ **取消编辑**：取消按钮放弃修改

### 3. 智能参数管理

#### 参数验证
- 自动过滤空的参数（key或value为空）
- 只保存有效的参数配置

#### 参数存储
- 用例参数存储在 `form.testCaseCustomParams` 对象中
- 格式：`{ testCaseId: [{ key: '', value: '' }] }`
- 支持多个用例的独立参数配置

#### 参数提交
- 表单提交时，将用例参数序列化为JSON
- 字段名：`testCaseCustomParams`
- 如果没有任何用例配置参数，则不发送该字段

### 4. 视觉设计

#### 表格列
- 用例参数列宽度：120px
- 居中对齐
- 设置图标大小：18px，绿色 (#67c23a)
- 徽章类型：success

#### 操作按钮
- 类型：主要按钮（primary）
- 大小：小号（small）
- 文本样式：text

#### 参数配置对话框
- 宽度：700px
- 参数行：左侧蓝色边框，灰色背景
- 序号标识：圆形，蓝色背景，白色数字
- 最大高度：400px（超出可滚动）

## 使用流程

### 新增策略时配置用例参数

```
1. 点击"新增策略"按钮
   ↓
2. 填写策略基本信息
   ↓
3. 选择用例集
   ↓
4. 在用例列表中点击"配置参数"按钮
   ↓
5. 在弹出对话框中添加/编辑参数
   ↓
6. 点击"保存"确认参数配置
   ↓
7. 重复步骤4-6配置其他用例
   ↓
8. 提交整个策略表单
```

### 编辑策略时修改用例参数

```
1. 点击策略行的"编辑"按钮
   ↓
2. 策略表单自动加载已有配置
   ↓
3. 已配置参数的用例显示参数徽章
   ↓
4. 点击"配置参数"修改用例参数
   ↓
5. 保存修改后提交策略表单
```

## 数据结构

### 前端表单数据

```javascript
form: {
  id: null,
  name: '',
  collectCount: 1,
  testCaseSetId: null,
  businessCategory: '',
  app: '',
  intent: '',
  customParams: [],              // 策略级别自定义参数
  testCaseCustomParams: {},      // 用例级别自定义参数
  description: '',
  status: 1,
}
```

### 用例参数格式

```javascript
testCaseCustomParams: {
  '1': [                          // 用例ID
    { key: 'timeout', value: '30' },
    { key: 'retryCount', value: '3' },
  ],
  '2': [
    { key: 'interval', value: '5' },
  ],
}
```

### 提交数据格式

```javascript
{
  name: '策略名称',
  collectCount: 5,
  testCaseSetId: 10,
  customParams: '[{"key":"env","value":"test"}]',           // JSON字符串
  testCaseCustomParams: '{"1":[{"key":"timeout","value":"30"}]}',  // JSON字符串
  // ... 其他字段
}
```

## 界面效果

### 用例列表表格

```
┌──────────────────────────────────────────────────────────────────────┐
│ 用例名称     │ 用例编号 │ 业务大类 │ APP  │ 逻辑组网  │ 用例参数 │ 操作 │
├──────────────────────────────────────────────────────────────────────┤
│ 视频通话测试  │ TC001   │ 通信    │ 微信 │ SA独立组网│  ⚙️ 2   │ 配置参数│
│ 文件传输测试  │ TC002   │ 文件    │ QQ  │ NSA非独立 │ 未配置   │ 配置参数│
│ ...          │ ...     │ ...     │ ... │ ...      │ ...     │ ...    │
└──────────────────────────────────────────────────────────────────────┘
```

### 参数配置对话框

```
┌─────────────────────────────────────────────────────┐
│         配置用例自定义参数                    [✕]    │
├─────────────────────────────────────────────────────┤
│                                                      │
│  用例信息：                                          │
│  ┌────────────────────────────────────────────┐    │
│  │ 用例名称: 视频通话测试 │ 用例编号: TC001   │    │
│  │ 业务大类: 通信         │ APP: 微信         │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│  ─────────────── 自定义参数 ───────────────        │
│                                                      │
│  参数列表                            [+ 添加参数]    │
│                                                      │
│  ┌────────────────────────────────────────────┐    │
│  │ ① [timeout        ] [30           ] [删除] │    │
│  │ ② [retryCount     ] [3            ] [删除] │    │
│  │ ③ [interval       ] [5            ] [删除] │    │
│  └────────────────────────────────────────────┘    │
│                                                      │
│                         [ 取消 ]  [ 保存 ]          │
└─────────────────────────────────────────────────────┘
```

## 技术实现

### 关键组件

#### 状态管理

```javascript
// 用例参数对话框状态
const testCaseParamsDialogVisible = ref(false)
const currentTestCase = ref(null)
const currentTestCaseParams = ref([])

// 表单数据
const form = reactive({
  // ...
  testCaseCustomParams: {},
})
```

#### 核心方法

```javascript
// 获取用例参数数量
const getTestCaseParamCount = (testCaseId) => {
  const params = form.testCaseCustomParams[testCaseId]
  return params ? params.length : 0
}

// 打开配置对话框
const handleConfigTestCaseParams = (testCase) => {
  currentTestCase.value = testCase
  const existingParams = form.testCaseCustomParams[testCase.id] || []
  currentTestCaseParams.value = JSON.parse(JSON.stringify(existingParams))
  testCaseParamsDialogVisible.value = true
}

// 保存用例参数
const saveTestCaseParams = () => {
  const validParams = currentTestCaseParams.value.filter(
    param => param.key && param.value
  )
  
  if (validParams.length > 0) {
    form.testCaseCustomParams[currentTestCase.value.id] = validParams
  } else {
    delete form.testCaseCustomParams[currentTestCase.value.id]
  }
  
  ElMessage.success(t('collectStrategy.saveParamsSuccess'))
  testCaseParamsDialogVisible.value = false
}
```

### 表单提交

```javascript
const handleSubmit = async () => {
  // ...
  const submitData = {
    // ...
    testCaseCustomParams: Object.keys(form.testCaseCustomParams).length > 0 
      ? JSON.stringify(form.testCaseCustomParams) 
      : null,
  }
  // ...
}
```

## 国际化支持

### 中文翻译键

```javascript
collectStrategy: {
  // ...
  testCaseCustomParams: '用例参数',
  configParams: '配置参数',
  configTestCaseParams: '配置用例自定义参数',
  testCaseParamList: '参数列表',
  noTestCaseParams: '暂无参数，点击"添加参数"按钮添加',
  configuredParams: '已配置 {count} 个参数',
  saveParamsSuccess: '保存成功',
}
```

### 英文翻译键

```javascript
collectStrategy: {
  // ...
  testCaseCustomParams: 'Test Case Parameters',
  configParams: 'Configure Parameters',
  configTestCaseParams: 'Configure Test Case Custom Parameters',
  testCaseParamList: 'Parameter List',
  noTestCaseParams: 'No parameters yet, click "Add Parameter" to add',
  configuredParams: 'Configured {count} parameters',
  saveParamsSuccess: 'Save successful',
}
```

## 修改文件清单

### 前端文件

1. **`DataCollectFront/src/views/collect-strategy/index.vue`**
   - ✅ 模板：添加用例参数列和配置对话框
   - ✅ 脚本：添加状态管理和方法
   - ✅ 样式：添加对话框样式

2. **`DataCollectFront/src/locales/zh.js`**
   - ✅ 添加中文翻译键

3. **`DataCollectFront/src/locales/en.js`**
   - ✅ 添加英文翻译键

### 后端要求（需配合后端开发）

后端需要：
1. 在采集策略实体中添加 `testCaseCustomParams` 字段（TEXT类型）
2. 在保存/更新策略时，处理 `testCaseCustomParams` JSON字符串
3. 在查询策略时，将 `testCaseCustomParams` JSON字符串解析为对象返回

## 使用场景示例

### 场景1：不同用例需要不同超时时间

```
用例1：视频通话测试
  - timeout: 60
  - retryCount: 3

用例2：文件传输测试
  - timeout: 120
  - retryCount: 5
  
用例3：消息发送测试
  - timeout: 30
  - retryCount: 2
```

### 场景2：特定用例需要特殊配置

```
用例1：性能压测
  - concurrentUsers: 100
  - duration: 300
  - rampUpTime: 60

用例2：功能测试
  - 不配置参数（使用默认值）

用例3：稳定性测试
  - duration: 86400
  - checkInterval: 60
```

### 场景3：环境相关参数

```
用例1：数据库连接测试
  - dbHost: 192.168.1.100
  - dbPort: 3306
  - dbName: testdb

用例2：API接口测试
  - apiEndpoint: http://test.api.com
  - apiVersion: v2
  - apiKey: xxx-xxx-xxx
```

## 优势与收益

### 灵活性

✅ 支持用例级别的精细化配置  
✅ 每个用例可以有不同的参数组合  
✅ 不影响其他用例的配置  

### 可维护性

✅ 参数配置集中管理，便于查看和修改  
✅ 界面直观，操作简单  
✅ 参数变更不需要修改代码  

### 扩展性

✅ 参数数量不受限制  
✅ 参数键值灵活定义  
✅ 支持后续添加更多参数类型  

## 注意事项

1. **参数命名规范**
   - 建议使用有意义的参数名
   - 避免使用特殊字符
   - 使用驼峰命名法（如 `maxRetryCount`）

2. **参数值格式**
   - 当前支持字符串类型
   - 数字请以字符串形式输入（如 `"30"`）
   - 布尔值请使用字符串（如 `"true"` 或 `"false"`）

3. **参数校验**
   - 前端仅验证参数键和值不为空
   - 具体参数的业务逻辑校验由后端执行

4. **性能考虑**
   - 建议每个用例的参数数量控制在合理范围（如不超过20个）
   - 大量用例配置参数时，建议分批操作

## 后续优化建议

1. **参数模板**
   - 支持保存常用参数组合为模板
   - 快速应用模板到多个用例

2. **批量配置**
   - 支持为多个用例批量配置相同参数
   - 支持参数的复制粘贴

3. **参数校验增强**
   - 添加参数值类型校验（数字、布尔等）
   - 添加参数值范围校验
   - 添加必填参数提示

4. **参数预设值**
   - 根据用例类型提供参数建议
   - 显示常用参数列表供选择

5. **参数历史记录**
   - 记录参数修改历史
   - 支持回退到之前的配置

## 修改时间

2024-10-14

## 相关文档

- 采集策略管理功能说明
- 自定义参数功能说明
- 用例集管理功能说明

