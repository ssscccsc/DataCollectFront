# 逻辑组网选择框数据源修改说明

## 修改概述

根据需求，将逻辑环境管理页面中的逻辑组网选择框的数据源从逻辑组网管理页面改为网络类型管理页面的数据。

## 修改内容

### 1. 数据源接口修改

**修改前：**
```javascript
// 加载逻辑组网选项
const loadNetworkOptions = async () => {
  const res = await request({
    url: '/logic-network/list',
    method: 'get',
  })
  networkOptions.value = res.data
}

// 加载可用组网
const loadAvailableNetworks = async () => {
  const res = await request({
    url: '/logic-network/list',
    method: 'get',
  })
  availableNetworks.value = res.data
}
```

**修改后：**
```javascript
// 加载网络类型选项
const loadNetworkOptions = async () => {
  const res = await request({
    url: '/network-type/list',
    method: 'get',
  })
  networkOptions.value = res.data
}

// 加载可用网络类型
const loadAvailableNetworks = async () => {
  const res = await request({
    url: '/network-type/list',
    method: 'get',
  })
  availableNetworks.value = res.data
}
```

### 2. 界面文本修改

| 修改项目 | 修改前 | 修改后 |
|---------|--------|--------|
| 表格列标题 | 逻辑组网 | 网络类型 |
| 表单标签 | 逻辑组网选择 | 网络类型选择 |
| 已选项标签 | 已选组网 | 已选网络类型 |
| 操作按钮 | 管理组网 | 管理网络类型 |
| 对话框标题 | 管理逻辑组网 | 管理网络类型 |
| 快速添加按钮 | 快速添加组网 | 快速添加网络类型 |
| 快速添加对话框标题 | 快速添加逻辑组网 | 快速添加网络类型 |

### 3. 快速添加功能修改

**修改前：**
- 创建逻辑组网记录
- 调用 `/logic-network` 接口

**修改后：**
- 创建网络类型记录
- 调用 `/network-type` 接口
- 默认状态设置为启用（status: 1）

### 4. 错误信息更新

所有相关的错误信息和成功消息都已更新为"网络类型"相关术语。

## 功能影响

### 正面影响
1. **数据一致性**：逻辑组网选择框现在使用网络类型管理页面的数据，确保数据来源统一
2. **用户体验**：用户可以在网络类型管理页面统一管理所有网络类型，然后在逻辑环境中选择使用
3. **数据管理**：避免了重复的网络类型定义，提高了数据管理的效率

### 注意事项
1. **后端接口**：需要确保 `/network-type/list` 接口存在并返回正确的数据格式
2. **数据迁移**：如果之前有逻辑组网数据，可能需要数据迁移或重新创建
3. **权限控制**：确保用户有访问网络类型管理页面的权限

## 测试建议

1. **功能测试**：
   - 验证逻辑组网选择框能正确加载网络类型数据
   - 测试快速添加网络类型功能
   - 测试网络类型管理对话框功能

2. **数据测试**：
   - 验证选择框显示的网络类型与网络类型管理页面一致
   - 测试新增网络类型后选择框能正确更新

3. **界面测试**：
   - 验证所有文本显示正确
   - 测试对话框和表单功能正常

## 后续优化建议

1. **数据缓存**：考虑对网络类型数据进行缓存，提高加载性能
2. **搜索功能**：为网络类型选择框添加搜索功能
3. **批量操作**：支持批量选择网络类型
4. **数据验证**：添加网络类型数据的有效性验证
