# 远程登录执行机功能说明

## 功能概述

在采集任务管理页面的任务详情页面中，为用例例次执行信息表格添加了"登录执行机"按钮，支持远程登录Linux和Windows执行机，并记录操作日志。

## 功能特性

### 1. 界面功能
- **登录按钮**：在用例例次执行信息表格的操作列中，在"查看详情"按钮后增加"登录执行机"按钮
- **条件显示**：只有当执行机IP存在时才显示登录按钮
- **按钮布局**：两个按钮并排显示，操作列宽度调整为200px

### 2. 远程登录弹窗
- **执行机信息展示**：显示IP地址和逻辑环境名称
- **操作系统选择**：支持Linux和Windows系统
- **连接方式选择**：
  - Linux系统：SSH、VNC
  - Windows系统：RDP
- **连接参数配置**：用户名、密码、端口号
- **操作说明**：必填字段，用于记录操作目的

### 3. 连接功能
- **SSH连接**：生成SSH命令并复制到剪贴板，显示连接信息对话框
- **RDP连接**：生成RDP URL并尝试打开RDP客户端
- **VNC连接**：生成VNC URL并尝试打开VNC客户端

### 4. 操作日志记录
- **后端API**：`/remote-login/log` 接口记录登录操作
- **日志字段**：执行机IP、逻辑环境、操作系统、连接方式、用户名、端口、操作说明、连接时间等
- **状态管理**：支持连接状态更新（CONNECTING、CONNECTED、DISCONNECTED、FAILED）

## 技术实现

### 1. 前端实现

#### 表格修改
```vue
<el-table-column label="操作" width="200">
  <template #default="scope">
    <div class="action-buttons">
      <el-button 
        v-if="scope.row.executionTaskId" 
        type="text" 
        size="small"
        @click="viewInstanceResult(scope.row)"
      >
        查看详情
      </el-button>
      <el-button 
        v-if="scope.row.executorIp" 
        type="text" 
        size="small"
        @click="openRemoteLoginDialog(scope.row)"
        style="margin-left: 8px;"
      >
        登录执行机
      </el-button>
    </div>
  </template>
</el-table-column>
```

#### 远程登录弹窗
```vue
<el-dialog
  v-model="remoteLoginDialogVisible"
  title="远程登录执行机"
  width="600px"
  :close-on-click-modal="false"
>
  <!-- 表单内容 -->
</el-dialog>
```

#### 连接逻辑
- **SSH**：生成命令并复制到剪贴板
- **RDP**：生成URL并尝试打开客户端
- **VNC**：生成URL并尝试打开客户端

### 2. 后端实现

#### 实体类
```java
@TableName("remote_login_log")
public class RemoteLoginLog {
    private Long id;
    private String executorIp;
    private String logicEnvironmentName;
    private String osType;
    private String connectionType;
    private String username;
    private Integer port;
    private String operationNote;
    private LocalDateTime connectTime;
    private LocalDateTime disconnectTime;
    private String status;
    // ... 其他字段
}
```

#### API接口
- **POST** `/remote-login/log` - 记录远程登录操作
- **PUT** `/remote-login/{id}/status` - 更新登录状态

#### 数据库表
```sql
CREATE TABLE `remote_login_log` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `executor_ip` varchar(50) NOT NULL,
  `logic_environment_name` varchar(100),
  `os_type` varchar(20) NOT NULL,
  `connection_type` varchar(20) NOT NULL,
  `username` varchar(50) NOT NULL,
  `port` int(11),
  `operation_note` text,
  `connect_time` datetime,
  `disconnect_time` datetime,
  `status` varchar(20) DEFAULT 'CONNECTING',
  -- ... 其他字段
);
```

## 使用流程

### 1. 用户操作流程
1. 进入采集任务管理页面
2. 点击任务名称查看详情
3. 在用例例次执行信息表格中找到需要登录的执行机
4. 点击"登录执行机"按钮
5. 填写远程登录表单：
   - 选择操作系统类型
   - 选择连接方式
   - 输入用户名和密码
   - 设置端口号
   - 填写操作说明
6. 点击"连接"按钮
7. 系统生成连接信息并执行连接

### 2. 连接方式说明

#### SSH连接（Linux）
- 生成SSH命令：`ssh username@ip -p port`
- 自动复制到剪贴板
- 显示连接信息对话框

#### RDP连接（Windows）
- 生成RDP URL：`rdp://ip:port`
- 尝试打开RDP客户端
- 如果失败则显示连接信息

#### VNC连接（Linux）
- 生成VNC URL：`vnc://ip:port`
- 尝试打开VNC客户端
- 如果失败则显示连接信息

## 安全考虑

### 1. 数据安全
- 密码字段使用`type="password"`和`show-password`属性
- 操作日志记录所有连接信息用于审计
- 支持连接状态跟踪

### 2. 权限控制
- 只有有执行机IP的记录才显示登录按钮
- 操作说明为必填字段，确保操作目的明确
- 记录操作时间和操作人信息

## 扩展功能

### 1. 可扩展的连接方式
- 支持更多连接协议（如Telnet、SFTP等）
- 支持自定义连接参数
- 支持连接模板保存

### 2. 操作记录增强
- 支持操作截图记录
- 支持操作命令记录
- 支持操作时长统计

### 3. 集成功能
- 集成Web终端（如xterm.js）
- 集成文件传输功能
- 集成批量操作功能

## 注意事项

1. **浏览器兼容性**：某些连接方式需要特定的浏览器支持
2. **网络环境**：确保执行机网络可达
3. **权限配置**：确保用户有相应的远程登录权限
4. **安全策略**：建议配置防火墙和访问控制策略

## 总结

远程登录执行机功能为采集任务管理提供了便捷的执行机访问能力，支持多种连接方式，并完整记录操作日志，提高了系统的可操作性和可审计性。该功能可以大大提升运维人员的工作效率，特别是在需要快速访问执行机进行问题排查或配置调整时。


