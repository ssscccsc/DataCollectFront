# 用例集目录视图功能说明

## 功能概述

在用例集详情页面新增**目录视图**功能，按照 **模型场景 → 业务大类 → APP** 的三级目录结构展示测试用例，方便用户快速浏览和定位用例。

## 功能特性

### 1. 双视图模式

页面右上角提供视图切换按钮：

- **目录视图**：按照三级目录结构分组展示
- **表格视图**：保留原有的表格平铺展示方式

### 2. 三级目录结构

#### 第一级：模型场景
- 图标：📁 文件夹图标（蓝色）
- 显示：模型场景名称 + 该场景下的用例总数
- 可展开/收起

#### 第二级：业务大类
- 图标：📂 打开文件夹图标（绿色）
- 显示：业务大类名称 + 该类别下的用例总数
- 可展开/收起

#### 第三级：APP
- 图标：📄 文档图标（橙色）
- 显示：APP名称 + 该APP下的用例数量
- 可展开/收起

### 3. 用例卡片展示

在最内层（APP级别）展开后，以卡片形式展示该分组下的所有测试用例，包含：

- **用例编号** + **用例名称**（醒目标题）
- **手机OS类型**
- **逻辑组网**（支持多个网络标签）
- **操作步骤**（可滚动查看）
- **预期结果**（可滚动查看）

### 4. 智能分组

- 自动按照三级结构分组
- 未配置字段显示为"未配置"分组
- 每个分组显示包含的用例数量统计

## 技术实现

### 数据分组算法

```javascript
// 计算属性：按照 模型场景 -> 业务大类 -> APP 进行分组
const groupedTestCases = computed(() => {
  const grouped = {}
  
  testCases.value.forEach(testCase => {
    const scenario = testCase.modelScenario || '未配置'
    const category = testCase.businessCategory || '未配置'
    const app = testCase.app || '未配置'
    
    // 创建三级嵌套结构
    if (!grouped[scenario]) {
      grouped[scenario] = {}
    }
    if (!grouped[scenario][category]) {
      grouped[scenario][category] = {}
    }
    if (!grouped[scenario][category][app]) {
      grouped[scenario][category][app] = []
    }
    
    grouped[scenario][category][app].push(testCase)
  })
  
  return grouped
})
```

### 组件使用

- **el-collapse**：三级嵌套折叠面板
- **el-card**：用例卡片展示
- **el-descriptions**：用例详细信息展示
- **el-tag**：标签展示（编号、网络类型等）

### 关键特性

1. **响应式分组**：使用Vue computed自动响应数据变化
2. **折叠状态管理**：支持独立控制每个层级的展开/收起
3. **用例数量统计**：实时统计各层级下的用例数量
4. **视觉层次**：通过缩进、颜色、图标区分层级

## 样式设计

### 目录层级视觉

```css
/* 第一级 - 蓝色 */
.collapse-title .el-icon {
  color: #409eff;
}

/* 第二级 - 绿色 */
.collapse-title.level-2 .el-icon {
  color: #67c23a;
}

/* 第三级 - 橙色 */
.collapse-title.level-3 .el-icon {
  color: #e6a23c;
}
```

### 嵌套缩进

每个子级向右缩进 24px，形成清晰的层级关系。

### 用例卡片

- 左侧彩色边框标识
- 悬停时阴影效果
- 灰色背景区分分组

## 使用场景

### 适用场景

✅ **大量用例**：用例数量较多时，目录视图更易于浏览  
✅ **分类明确**：用例按照模型场景、业务大类、APP分类清晰  
✅ **快速定位**：需要快速找到特定分类下的用例  

### 切换场景

🔄 **表格视图**：需要对比多个字段、导出数据等操作  
🔄 **目录视图**：浏览用例、理解分类结构、查看详情  

## 界面效果

```
📊 测试用例 (150)  [目录视图 | 表格视图]

📁 模型场景: 5G通信 (80个用例)
  └─ 📂 业务大类: 视频通话 (50个用例)
      └─ 📄 APP: 微信 (30个用例)
          ┌─────────────────────────────────┐
          │ [001] 微信视频通话连接测试       │
          │ 手机OS: Android                  │
          │ 逻辑组网: SA独立组网              │
          │ 操作步骤: ...                     │
          │ 预期结果: ...                     │
          └─────────────────────────────────┘
      └─ 📄 APP: QQ (20个用例)
  └─ 📂 业务大类: 文件传输 (30个用例)

📁 模型场景: 4G通信 (70个用例)
  └─ ...
```

## 优势对比

| 特性 | 表格视图 | 目录视图 |
|------|---------|----------|
| 数据密度 | 高 | 中 |
| 浏览友好度 | 中 | 高 |
| 分类清晰度 | 低 | 高 |
| 详情展示 | 需滚动 | 卡片直观 |
| 快速定位 | 搜索/筛选 | 展开分组 |
| 大数据量 | 需分页 | 分组折叠 |

## 修改文件

- `DataCollectFront/src/views/test-case-set/detail.vue`

## 修改内容

### Template 部分
- ✅ 新增视图切换按钮
- ✅ 新增三级折叠面板结构
- ✅ 新增用例卡片展示
- ✅ 保留原有表格视图

### Script 部分
- ✅ 新增 `viewMode` 状态（默认目录视图）
- ✅ 新增 `groupedTestCases` 计算属性（分组数据）
- ✅ 新增折叠状态管理
- ✅ 新增统计方法

### Style 部分
- ✅ 新增目录视图样式
- ✅ 新增用例卡片样式
- ✅ 新增层级颜色标识

## 注意事项

1. **性能考虑**：使用计算属性确保分组数据响应式且性能优化
2. **空数据处理**：未配置的字段统一归类到"未配置"分组
3. **用户体验**：默认展示目录视图，用户可自由切换
4. **兼容性**：保留原有表格视图，确保向下兼容

## 后续优化建议

1. **搜索功能**：在目录视图中添加搜索高亮
2. **筛选功能**：支持按分类快速筛选
3. **全部展开/收起**：提供一键展开/收起所有分组
4. **记忆功能**：记住用户的视图选择和展开状态
5. **导出功能**：支持按分组导出用例数据

## 修改时间

2024-10-14

