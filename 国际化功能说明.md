# DataCollectFront 中英文切换功能说明

## 功能概述

本项目已成功集成 Vue I18n 国际化功能，支持中英文切换。用户可以通过页面右上角的语言切换器在中文和英文之间自由切换，系统会自动保存用户的语言偏好。

## 技术实现

### 1. 依赖安装
- 已添加 `vue-i18n@9.2.2` 依赖到 `package.json`

### 2. 文件结构
```
src/
├── locales/
│   ├── zh.js          # 中文语言包
│   └── en.js           # 英文语言包
├── i18n/
│   └── index.js        # i18n 配置文件
├── components/
│   └── LanguageSwitcher.vue  # 语言切换组件
└── ...
```

### 3. 核心功能

#### 语言包管理
- **中文语言包** (`src/locales/zh.js`): 包含所有中文翻译
- **英文语言包** (`src/locales/en.js`): 包含所有英文翻译
- 支持嵌套结构，便于管理不同模块的翻译

#### 语言切换组件
- 位置：页面右上角，用户信息下拉菜单旁边
- 功能：点击可切换中英文
- 样式：带有国旗图标的美观下拉菜单
- 持久化：语言选择会保存到 localStorage

#### 国际化集成
- **布局组件**: 侧边栏菜单、页面标题、用户信息等全部支持国际化
- **路由配置**: 路由元信息使用国际化键值
- **页面组件**: 仪表盘等页面组件已集成国际化

## 使用方法

### 1. 在组件中使用翻译
```vue
<template>
  <div>
    <!-- 使用 $t() 函数进行翻译 -->
    <h1>{{ $t('pageTitle.dashboard') }}</h1>
    <p>{{ $t('dashboard.description') }}</p>
  </div>
</template>

<script>
import { useI18n } from 'vue-i18n'

export default {
  setup() {
    const { t } = useI18n()
    
    // 在 JavaScript 中使用翻译
    const message = t('common.success')
    
    return {
      message,
    }
  },
}
</script>
```

### 2. 添加新的翻译
在 `src/locales/zh.js` 和 `src/locales/en.js` 中添加新的翻译键值对：

```javascript
// zh.js
export default {
  // ... 现有翻译
  newModule: {
    title: '新模块标题',
    description: '新模块描述',
  },
}

// en.js
export default {
  // ... 现有翻译
  newModule: {
    title: 'New Module Title',
    description: 'New Module Description',
  },
}
```

### 3. 语言切换
用户可以通过以下方式切换语言：
1. 点击页面右上角的语言切换器
2. 选择目标语言（中文/English）
3. 页面会自动刷新并应用新语言

## 已实现的翻译内容

### 通用翻译
- 按钮文本：确认、取消、保存、编辑、删除等
- 状态文本：成功、失败、警告、提示等

### 导航菜单
- 仪表盘、数据采集、设置等主菜单
- 所有子菜单项：采集任务管理、执行机管理、UE管理等

### 页面标题
- 所有页面的标题都支持国际化

### 仪表盘页面
- 统计卡片标签
- 表格列标题
- 系统信息标签
- 任务状态文本

## 扩展说明

### 添加新页面国际化
1. 在语言包中添加新页面的翻译
2. 在页面组件中使用 `$t()` 函数
3. 在路由配置中更新 meta.title 为国际化键值

### 添加新语言
1. 创建新的语言包文件（如 `src/locales/ja.js`）
2. 在 `src/i18n/index.js` 中导入并添加到 messages
3. 在语言切换组件中添加新语言选项

### 最佳实践
1. 使用有意义的键名，如 `menu.dashboard` 而不是 `menu1`
2. 保持中英文翻译的一致性
3. 对于动态内容，考虑使用插值功能
4. 定期检查未翻译的文本

## 注意事项

1. **语言切换会刷新页面**: 为了确保所有组件正确应用新语言，切换语言时会刷新页面
2. **语言偏好持久化**: 用户的语言选择会保存到 localStorage，下次访问时会自动应用
3. **默认语言**: 系统默认使用中文，如果 localStorage 中没有保存语言设置
4. **兼容性**: 支持现代浏览器，需要支持 localStorage 和 ES6+ 语法

## 测试建议

1. 测试语言切换功能是否正常工作
2. 检查所有页面文本是否正确翻译
3. 验证语言偏好是否正确保存和恢复
4. 测试页面刷新后语言设置是否保持























